.venv
.DS_Store
__pycache__
*.egg-info/
*.code-workspace
.git_corrupt_*
ref/*
cache
# `artifacts/*` rather than `artifacts`: git cannot re-include anything below an
# EXCLUDED DIRECTORY, so the bare form would make the negation below a no-op.
artifacts/*
# ...except the verification harness output, which must be committed. It carries
# the producing commit SHA and the hash of the compiled Rust extension; a pillar
# result a reader cannot trace to code is not evidence. Same reasoning as
# doc/results/ below.
!artifacts/verify/

# IDE / tool dirs
.specstory/
.cursor/
.claude/
.openclaude-profile.json

# Generated figures and visualizations (regenerable from code)
figures/*.png
figures/*.mp4
figures/*.html
figures/*.json
figures/*.pkl

# Root-level generated data
*.pkl

# Generated Dymos / OpenMDAO outputs
*_out/

# Syncthing conflict files
*.sync-conflict-*

# Experiment results (regenerable from scripts)
results/
# ...except the paper's generated tables, which must be committed: they carry the
# producing commit SHA, and numbers a reader cannot trace to code are not evidence.
!doc/results/

# Specific excludes
doc/20250902_*

# Reference papers
*.pdf
rust_optimizer/target
doc/career_profile_and_advice.md

# etc/sidetrack
forethoughs

# LaTeX build artifacts (regenerable from .tex sources)
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.lof
*.lot
*.nav
*.snm
*.vrb

# paper draft backups (created before each revision edit batch)
doc/*.bak-*
